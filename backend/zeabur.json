{
  "name": "wellmade-backend",
  "build": {
    "buildCommand": "npm install && npm run build && npm run migration:run",
    "outputDirectory": "dist"
  },
  "run": {
    "startCommand": "NODE_OPTIONS=--experimental-global-webcrypto npm run start:prod"
  }
}