# SKU 自動生成功能 - 現有資料處理指南

## 問題說明
在實作 SKU 自動生成功能之前，系統中可能已經存在一些沒有 SKU 的產品變體。本指南說明如何處理這些現有資料。

## 解決方案

### 方案一：透過 UI 介面處理（推薦）

1. **單一變體處理**
   - 進入產品編輯頁面
   - 點擊需要設定 SKU 的變體旁的「編輯」按鈕
   - 系統會自動偵測該變體沒有 SKU，並預設開啟「自動生成」
   - 點擊「儲存」即可

2. **批量處理**
   - 進入產品編輯頁面
   - 如果有變體缺少 SKU，頁面上方會出現黃色提示框
   - 點擊「批量生成 SKU」按鈕
   - 系統會為所有缺少 SKU 的變體自動生成

### 方案二：使用資料庫腳本（技術人員使用）

執行以下命令來批量生成缺失的 SKU：

```bash
cd backend
npx ts-node src/scripts/generate-missing-skus.ts
```

這個腳本會：
- 查找所有沒有 SKU 的變體
- 根據產品的品牌、分類和規格自動生成 SKU
- 更新資料庫中的記錄

## SKU 生成規則

生成的 SKU 格式為：`[品牌]-[分類]-[產品ID]-[規格]`

範例：
- `KIT-KCH-A1B2-WH-L` (KitchenAid 廚房用品，白色，大號)
- `IKEA-BED-C3D4-WD` (IKEA 臥室用品，木質)

## 注意事項

1. **唯一性保證**：系統會自動確保生成的 SKU 不會重複
2. **可編輯性**：自動生成的 SKU 仍然可以手動修改
3. **向後相容**：已有 SKU 的變體不會被影響

## 功能特點

- 🔄 **智能切換**：可隨時在「自動生成」和「手動輸入」之間切換
- 👀 **即時預覽**：輸入規格時立即看到 SKU 預覽
- ⚡ **批量處理**：一鍵為所有缺失的變體生成 SKU
- 🎯 **視覺提示**：缺少 SKU 的變體會有明顯的紅色提示